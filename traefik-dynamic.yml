http:
  middlewares:
    redirect-https:
      redirectScheme:
        scheme: https
        permanent: true

    vbrowser-auth:
      forwardAuth:
        address: "http://vbrowser-backend:8080/auth/verify"
        authResponseHeaders:
          - "X-Forwarded-User"
        trustForwardHeader: true
